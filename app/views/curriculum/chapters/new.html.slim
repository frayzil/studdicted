h1 New curriculum chapter for #{@branch.title}

= simple_form_for([@branch, @chapter], url: :curriculum_branch_chapters) do |f|
  == render 'form', f: f, back_to: curriculum_branch_chapters_path(@branch)
