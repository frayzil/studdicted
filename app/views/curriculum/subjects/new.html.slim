h1 New curriculum subject for #{@board.name}

= simple_form_for([@board, @subject], url: :curriculum_board_subjects) do |f|
  == render 'form', f: f

= link_to 'Back', curriculum_board_subjects_path(@board)
